# 导航方向 · 提交报告

> **作业**：《2027赛季算法组培训考核作业细则》§3 导航方向
> **提交仓库**：https://github.com/gebilaowuxiaoyu123/RMCS_Navigation
> **报告状态**：🚧 编写中（§0 摘要标注了各项完成度）
> **过程规划**：[任务规划.md](任务规划.md)

---

## 0. 摘要

### 0.1 一句话结论

> 将哨兵的全局规划器从 `NavfnPlanner` 换成 **`SmacPlanner2D`**。理由：NavFn 输出的是格子级折线路径且
> 单分辨率全图搜索，Smac2D 具备**内置路径平滑 + 多分辨率降采样 + 显式障碍代价**三项能力，
> 正对"路径转折导致跟踪反复降速"和"大地图规划耗时"这两个卡顿来源。

### 0.2 完成度

| 项 | 状态 | 说明 |
|---|---|---|
| 依赖栈与环境搭建 | ✅ 完成 | `rmcs-navigation-deps` + 6 子模块；补齐 `nav2-mppi-controller`、`py-trees` |
| 基线确认 | ✅ 完成 | 现用 `NavfnPlanner`（`ros2 param get` 实测） |
| 地图确认 | ✅ 完成 | `rmuc-v2.png`，292×161 px @ 0.1 m ≈ 29.2×16.1 m |
| 无机器人测试环境 | ✅ 完成 | `static.launch.yaml` 4 节点全 `active` |
| 规划器切换 | ⏳ 待做 | — |
| 调参消除卡顿 | ⏳ 待做 | — |
| 跨场验证 | ⏳ 待做 | — |
| 基线 / 对比录屏 | ⏳ 待做 | — |

### 0.3 验收对照

| 细则验收项 | 对应章节 | 状态 |
|---|---|---|
| ① 讲为什么挑这个规划器、与现有差异 | §2 方案选型 | ⏳ 论证已完成，待补实测对比 |
| ② 效果演示 + 代码提交 GitHub | §4 结果、§6 提交信息 | ⏳ 待补 |
| ③ 讲实际调试的调参思路 | §3.3 调参方案 | ⏳ 方案已完成，待补实测记录 |

---

## 1. 任务与要求

### 1.1 细则原文

| 项 | 内容 |
|---|---|
| **目标** | 使用 `rmcs-navigation-deps` 控制哨兵，挑选一个全局规划器（**不能用现在的 NavfnPlanner**），做到在实际场地的**跨场导航** |
| **要求 1** | 要切换全局规划器，地图会给出 |
| **要求 2** | 独立完成，通过调整规划器参数，实现导航过程中**无明显卡顿** |
| **验收 1** | 讲讲**为什么挑选这个规划器**，它和我们现有规划器的差异 |
| **验收 2** | 效果演示，代码提交 GitHub |
| **验收 3** | 讲讲在实际调试过程中的**调参思路** |

### 1.2 拆解

| 细则要求 | 实际工程动作 | 工作量 |
|---|---|---|
| 挑选全局规划器 | 在 `motion.yaml` 换插件类名 | 小 |
| 调参消除卡顿 | 调 5~8 个数值参数 | **主要工作量** |
| 实际场地跨场导航 | 整张场地图跑通跨场 | 需地图 + 实车 |
| 讲选型 / 差异 / 调参思路 | 把过程讲清楚 | 需自圆其说 |

> 任务本质是 **"配置工程 + 参数整定 + 表达"**，不是写算法。全部代码改动集中在 yaml 配置。

---

## 2. 方案选型

### 2.1 基线：现在的规划器确实是 NavfnPlanner

细则表述**正确**，实测确认：

```yaml
# rmcs-navigation/config/motion.yaml
planner_server:
  ros__parameters:
    planner_plugins: ["GridBased"]
    GridBased:
      plugin: "nav2_navfn_planner::NavfnPlanner"   # ← 现在的规划器
      tolerance: 0.5
      use_astar: true
      allow_unknown: true
      use_final_approach_orientation: false
```

```bash
$ ros2 param get /planner_server GridBased.plugin
String value is: nav2_navfn_planner::NavfnPlanner
```

**一次误判的记录**：首次拉取时，父仓库 `rmcs-navigation-deps` pin 的子模块是
`d57fa1c`（2026-05-22），该版本规划器为 `ThetaStarPlanner`，一度误以为细则过时。
实际是 **pin 的版本太旧**。查历史可见规划器被主动改回过 NavFn：

```
$ git log -p d57fa1c..main -- config/motion.yaml | grep -E '^[+-].*plugin:'
commit 3663d5f  wip: localization api, refactor impl and adjust nav2 config
-      plugin: "nav2_theta_star_planner::ThetaStarPlanner"
+      plugin: "nav2_navfn_planner::NavfnPlanner"
```

**教训**：判断"现在用什么"要以**上游最新分支**为准，不能只看父仓库 pin 的 commit。

### 2.2 候选对比

| 规划器 | 原理 | 优点 | 缺点 |
|---|---|---|---|
| `NavfnPlanner`（**基线**） | 代价地图上 Dijkstra/A\* 势场搜索，单分辨率全图 | 稳、老牌、行为可预测 | ① 输出格子级折线、**无平滑**，转折多，跟踪反复降速；② 视机器人为**质点**，安全裕度全靠膨胀；③ 单分辨率全图搜索，大地图耗时明显 |
| `ThetaStarPlanner`（曾用过，`3663d5f` 换回） | A\* + 视线检测，可走任意角度 | 路径短、直、转折少 | **无内置平滑器**；安全裕度靠膨胀硬撑 |
| **`SmacPlanner2D`** ✅ 选中 | A\* + **多分辨率降采样** + 显式障碍代价 + **内置平滑器** | 路径平滑；搜索快；可调项多 | 需调 smoother；栅格粗时路径偏糙 |
| `SmacPlannerHybrid` | 2D + Dubin/Reeds-Shepp 运动学 | 满足最小转弯半径 | 哨兵为全向底盘，**不需要**；参数多易无解 |
| `SmacPlannerLattice` | 状态格 + 运动基元 | 最贴运动学 | 配置最复杂，新手不宜 |

### 2.3 决策过程

```
5 个候选
  ↓ 排除"细则禁用"的 NavFn
剩 4 个
  ↓ 排除需要运动学模型的（哨兵全向底盘，无转弯半径限制）→ Hybrid、Lattice
剩 2 个：ThetaStarPlanner、SmacPlanner2D
  ↓ 对比：Smac2D 有内置平滑器 + 多分辨率降采样，正治"卡顿"
结论：SmacPlanner2D
```

### 2.4 选型理由（验收 ① 的答案）

**选 `nav2_smac_planner::SmacPlanner2D`**，三条理由：

1. **内置路径平滑器**（`smoother`）。NavFn 输出格子级折线，每个转折点附近速度几乎掉到 0，
   跟踪起来就是"一格一格顿"；Smac2D 自带平滑，无需自写后处理。
   → **这是"无明显卡顿"最直接的一层。**
2. **多分辨率降采样**（`downsampling_factor`）：先在粗网格搜、再细化，大地图上比
   NavFn 的单分辨率全图搜索快得多。→ **第二层。**
3. **显式障碍代价**（`cost_penalty`）：搜索时把"离障碍远近"折算进 f，把路径更有力地推到
   通道中间，实车不必频繁避障微调。

**与现有规划器的差异（一句话说清）**：

| 维度 | NavFn（现状） | SmacPlanner2D |
|---|---|---|
| 路径输出 | 格子级折线，无平滑 | 内置平滑器输出平顺曲线 |
| 搜索方式 | 单分辨率全图 | 多分辨率降采样，先粗后细 |
| 障碍代价 | 隐式（靠膨胀层） | 显式 `cost_penalty` 可调 |
| 大地图耗时 | 高 | 明显更低 |

> **一句话版本**：*选 Smac2D，因为它内置路径平滑 + 多分辨率加速 + 显式障碍代价，
> 同时解决 NavFn 折线转折导致的反复降速和单分辨率全图搜索的耗时。*

⏳ 待验证：`SmacPlanner2D` 在 0.1 m 粗栅格 + 0.5 m 膨胀下，窄门洞是否仍可通行。

### 2.5 关键概念：代价地图

Nav2 代价地图不是 0/1 两态，而是 **0~254** 多级：

| 代价 | 含义 |
|---|---|
| `254` | 致命（lethal）——真实障碍 |
| `253` | 内切（inscribed）——机器人中心到这必撞 |
| 1~252 | 膨胀区——离障碍越近代价越高 |
| `0` | 完全自由 |

衰减公式（$k$ = `cost_scaling_factor`）：

$$\text{cost}(d) = 253 \cdot e^{-k \cdot d}$$

**意义**：$f = g + h$ 中，走通道中间（代价 0）比贴墙（代价 200）f 更小，
算法自动偏好通道中间。这正是"让 f 有区分度"的手段。

---

## 3. 实施

### 3.1 环境搭建

| 项 | 内容 |
|---|---|
| 依赖栈 | `rmcs_ws/src/rmcs-navigation-deps/`，6 个子模块：`rmcs-navigation`、`rmcs-local-map`、`rmcs-localization`、`point-lio`、`livox-ros-driver2`、`odin-ros-driver` |
| 补齐依赖 | `ros-jazzy-nav2-mppi-controller`、`ros-jazzy-py-trees` |
| 可用规划器 | `navfn` / `theta_star` / `smac`（2D、Hybrid、Lattice）均已安装 |
| CMake | 需 ≥ 3.30，使用 `/opt/cmake/bin/cmake`（4.2.3），系统自带 3.28 过旧 |

**基线偏离说明**：工作分支基于**上游 `main`（`632962c`）**，而非父仓库 pin 的 `d57fa1c`。
原因：`d57fa1c` 缺少 `<depend>rmcs_description</depend>`，**无法编译**：

```
fatal error: rmcs_description/sentry_description.hpp: No such file or directory
```

上游 `main` 的 `632962c` 提交正是 "fix: declare rmcs_description dependency"，
且细则所指的 NavFn 只存在于 `main` 上。

### 3.2 无机器人测试环境

`rmcs-navigation/launch/static.launch.yaml` 专为纯软件联调设计，
**不需要任何真实传感器/里程计/SLAM**。

启动：mock `local_map`（`empty.yaml`）、`global_map`、静态 TF（`world→odom→base_link`）、
`motion.launch.yaml` 全部导航节点、`foxglove_bridge`。

**不启动**：传感器驱动、SLAM/定位、`rmcs_local_map`、`point_lio`。

```bash
# 构建
bash -lc 'export PATH=/opt/cmake/bin:$PATH && cd /workspaces/RMCS/rmcs_ws \
  && source /opt/ros/jazzy/setup.bash && source install/setup.bash \
  && colcon build --packages-up-to rmcs-navigation --symlink-install'

# 启动（无需 C 板、无需雷达）
bash -lc 'cd /workspaces/RMCS/rmcs_ws && source install/setup.bash \
  && ros2 launch rmcs-navigation static.launch.yaml'
```

实测结果（4 节点全部 `active`）：

```
/bt_navigator          active [3]
/planner_server        active [3]
/controller_server     active [3]
/global_map            active [3]
```

**这个环境的价值**：不依赖机器人即可验证规划器切换是否生效、规划速度是否达标。

### 3.3 调参方案

#### 3.3.1 先判断"卡顿"是哪一种

| 类型 | 现象 | 根因 | 测量 |
|---|---|---|---|
| **规划卡顿** | 走到一半停住几秒再走 | 全局规划慢，`/plan` 迟迟不出 | `ros2 topic hz /plan` 看空档 |
| **跟踪卡顿** | 一直在走但"一顿一顿" | 路径折线，转折处反复降速 | Foxglove 看 `/cmd_vel` 速度曲线 |

**必须先判断类型**，否则会瞎调。

```bash
ros2 topic hz /plan
ros2 param get /planner_server GridBased.plugin    # 确认参数真生效
```

> 前人踩过的坑：`doc/adjustment-navigation.md` 明确写着"远端联调必须流程化，
> 否则很容易被『假生效』误导"。**调完参数一定回读确认。**

#### 3.3.2 调参顺序：从粗到细，一次只动一个

| 顺序 | 参数 | 现值 | 作用 | 调过头的现象 |
|---|---|---|---|---|
| 1 | `inflation_radius` | **0.5** | 障碍周围安全圈大小 | **门洞堵死 → 规划失败** |
| 2 | `cost_scaling_factor` | **0.5** | 代价衰减快慢 | 调小 → 路径贴墙 |
| 3 | `cost_penalty`（Smac2D） | — | 障碍代价权重 | 过大 → 绕远 |
| 4 | `downsampling_factor`（Smac2D） | — | 降采样倍数 | 过大 → 路径粗糙 |
| 5 | `tolerance` | **0.5** | 终点允许偏差 | 过小 → 终点附近无解 |
| 6 | `smoother.w_smooth` / `w_data` | — | 平滑度 / 贴合原始路径 | `w_data` 过小 → 平滑到撞障碍 |
| 7 | 重规划频率（`motion.xml`） | **1.0 Hz** | 重规划频率 | 过高 → 频繁重规划＝明显卡顿 |

#### 3.3.3 调参记录

> 每次改动记录：改动 → 原因 → 现象 → 结论。**以下是模板，实测数据待补。**

| # | 参数 | 改动 | 原因 | 现象 | 结论 |
|---|---|---|---|---|---|
| — | — | — | — | — | ⏳ 待补 |

格式示例：

```
改动: inflation_radius 0.5 -> 0.35
原因: 0.5 m 在 0.1 m 栅格上占 5 格, RMUC 狗洞被膨胀堵死, 规划失败
现象: 规划成功率恢复; /plan 输出恢复稳定
结论: 保留 0.35
```

**数值参数可在线试**（无需重编译）：

```bash
ros2 param set /planner_server GridBased.tolerance 0.2
ros2 param get /planner_server GridBased.tolerance     # 回读确认
```

> ⚠️ 换插件类名**必须改 yaml + 重启**（启动时确定），`ros2 param set` 只能调数值参数。

#### 3.3.4 调参时的两个观察点

现状参数中有两处值得注意的不一致，已列为优先排查项：

| 项 | 现状 | 问题 |
|---|---|---|
| **全局 / 局部 footprint** | 全局 `[[0.1,…]]`（0.2 m 见方）<br>局部 `[[0.2475,…]]`（0.495 m 见方） | 差 2.5 倍。真实底盘由"斜边 0.7 m"换算为 0.2475（见 `doc/adjustment-navigation.md` §6），`0.1` 更像占位值。全局图偏小会让**全局路径过于贴近障碍** |
| **膨胀半径** | `0.5 m` | 0.1 m 栅格下仅占 5 格，而车宽 0.495 m 也占约 5 格 → 窄门洞（"狗洞"）有**膨胀堵死导致无解**风险 |

**判据**：通道可通行条件 ≈ 通道宽度 > 车宽 + 2×膨胀 = 0.495 + 1.0 = **1.495 m**。
窄于 1.5 m 的通道存在无解风险 —— 这是首要体检目标。

---

## 4. 结果

### 4.1 环境验证（已完成）

| 检查项 | 命令 | 实测结果 |
|---|---|---|
| 节点启动 | `ros2 node list` | 4 节点齐全 |
| 生命周期 | `ros2 lifecycle get /<node>` | 全部 `active [3]` |
| 当前规划器 | `ros2 param get /planner_server GridBased.plugin` | `nav2_navfn_planner::NavfnPlanner` |

### 4.2 规划器对比（待补）

> ⏳ 待做：同一组起终点，分别用 NavFn / SmacPlanner2D 跑，记录下表。

| 指标 | NavFn（基线） | SmacPlanner2D | 测量方式 |
|---|---|---|---|
| `/plan` 输出频率 | — | — | `ros2 topic hz /plan` |
| 单次规划耗时 | — | — | 节点日志 / 计时 |
| 路径转折点数 | — | — | 路径 message 统计 |
| 路径平滑度 | — | — | 相邻点夹角统计 |
| 门洞可通行 | — | — | 设终点在门洞对面，看是否成功 |

### 4.3 演示材料（待补）

> ⏳ 待做：基线录屏 + 切换后录屏，放入 `assets/`。

---

## 5. 问题与解决

| # | 问题 | 原因 | 解决 |
|---|---|---|---|
| 1 | zsh 下 `source /opt/ros/jazzy/setup.bash` 静默失效，报 `no such file: .../rmcs_ws/setup.sh` | 该脚本用 `BASH_SOURCE[0]` 定位自身，zsh 下该变量为空 | 用 `bash -lc '...'` 包一层执行 |
| 2 | `CMake 3.30 or higher is required. You are running version 3.28.3` | 系统 CMake 过旧 | 用 `/opt/cmake/bin`（4.2.3）：`export PATH=/opt/cmake/bin:$PATH` |
| 3 | `build-rmcs` 报 `install directory was created with the layout 'isolated'` | 该脚本默认 `--merge-install`，与现有 isolated 布局冲突 | 改用 `colcon build --symlink-install` |
| 4 | `rmcs_description/sentry_description.hpp: No such file or directory` | 父仓库 pin 的 `d57fa1c` 缺 `<depend>rmcs_description</depend>` | 切到上游 `main`（`632962c` 已修复） |
| 5 | 依赖缺失导致 `controller_server` 起不来 | 镜像未带 `nav2-mppi-controller`、`py-trees` | `apt-get install ros-jazzy-nav2-mppi-controller ros-jazzy-py-trees` |
| 6 | 一度误判"现在的规划器是 ThetaStar，细则过时" | 读了父仓库 pin 的旧 commit | 以**上游 main** 为准；`git log -p` 查变更历史确认 |

---

## 6. 提交信息

### 6.1 仓库与分支

| 位置 | 内容 | 地址 |
|---|---|---|
| 本报告与规划文档 | 提交报告、过程规划、配置快照 | https://github.com/gebilaowuxiaoyu123/RMCS_Navigation |
| 代码改动 | `rmcs-navigation` 的 `motion.yaml` 等 | fork `gebilaowuxiaoyu123/rmcs-navigation`，分支 `feat/smac-global-planner` |
| 上游参考 | 官方仓库 | `Alliance-Algorithm/rmcs-navigation`（remote 名 `upstream`，仅拉更新） |

### 6.2 改动文件

| 文件 | 改动 |
|---|---|
| `rmcs-navigation/config/motion.yaml` | `GridBased.plugin`：NavFn → SmacPlanner2D；新增 Smac2D 参数；调整 `inflation_radius` 等 |
| `rmcs-navigation/config/motion.xml` | （如需）重规划频率 |

> ⏳ 待做：改动落盘后补完整的 diff 与 commit 链接。

---

## 7. 参考

| 资料 | 位置 |
|---|---|
| 前人调参复盘（DWB→MPPI 全过程、报错速查表、footprint 换算） | `rmcs-navigation/doc/adjustment-navigation.md` |
| 导航系统说明（架构、Lua 决策、调用链） | `rmcs-navigation/README.md` |
| 导航调试 SOP（远端重启、生命周期检查、参数回读） | `rmcs-navigation/AGENTS.md` |
| 官方 NavFn 规划器文档 | Nav2 官方文档 `nav2_navfn_planner` |
| 官方 Smac Planner 文档 | Nav2 官方文档 `nav2_smac_planner` |

---

## 附录 A：关键事实速查

| 项 | 值 |
|---|---|
| 当前规划器 | `nav2_navfn_planner::NavfnPlanner`（`tolerance: 0.5`, `use_astar: true`） |
| 目标规划器 | `nav2_smac_planner::SmacPlanner2D` |
| 默认地图 | `maps/rmuc.yaml` → `rmuc-v2.png`，292×161 px @ 0.1 m ≈ 29.2×16.1 m |
| 全局代价地图分辨率 | 未显式指定，继承地图 0.1 m |
| 全局 / 局部 footprint | 0.2 m 见方 / 0.495 m 见方（⚠️ 不一致） |
| 膨胀半径 | `0.5 m`（全局、局部同值） |
| 局部规划器 | `MPPIController`，`motion_model: Omni`（已调好，不动） |
| 重规划频率 | `1.0 Hz`（行为树 `RateController`） |
| 目标容差 | `xy_goal_tolerance: 0.1` |

## 附录 B：常用命令

```bash
# 构建
bash -lc 'export PATH=/opt/cmake/bin:$PATH && cd /workspaces/RMCS/rmcs_ws \
  && source /opt/ros/jazzy/setup.bash && source install/setup.bash \
  && colcon build --packages-select rmcs-navigation --symlink-install'

# 启动静态栈（无机器人）
bash -lc 'cd /workspaces/RMCS/rmcs_ws && source install/setup.bash \
  && ros2 launch rmcs-navigation static.launch.yaml'

# 换地图
bash -lc 'cd /workspaces/RMCS/rmcs_ws && source install/setup.bash \
  && ros2 launch rmcs-navigation static.launch.yaml global_map:=战队'

# 检查
bash -lc 'cd /workspaces/RMCS/rmcs_ws && source install/setup.bash && \
  ros2 node list && \
  ros2 param get /planner_server GridBased.plugin && \
  ros2 topic hz /plan'

# 在线调参
bash -lc 'cd /workspaces/RMCS/rmcs_ws && source install/setup.bash && \
  ros2 param set /planner_server GridBased.cost_penalty 4.0'

# 清理残留
bash -lc 'pkill -f "ros2 launch rmcs-navigation"; pkill -f nav2_; sleep 1; echo done'
```
