# 实验记录

> 日志化记录：**每次执行了什么、看到什么、得出什么**。
> 结论汇总见 [导航提交作业.md](导航提交作业.md)；操作方法见 [任务规划.md](任务规划.md)。
>
> 写法约定：每次实验加一节，标题为 `## 日期 时间 · 一句话主题`；
> 内部按「执行 → 结果 → 问题 → 结论」四段写，命令和数据尽量原文保留。

---

## 2026-09-22 21:05 – 21:25 · 跑通静态导航栈，取得 NavFn 基线数据

### 执行

> 📝 **命令说明**：下面记录的是当时的原始命令，都带了 `bash -lc` 和 `source` 前缀。
> 后来发现**这些前缀完全没必要** —— `.zshrc` 已自动 source 好环境
> （`~/env_setup.zsh`），`ros2`/`colcon` 直接可用。
> 简化后的写法见 [任务规划.md](任务规划.md) §2.2。此处保留原样以存真。

1. **清理残留进程**（第一次启动发现节点重复，才回头做这步）
   ```bash
   pkill -f 'ros2 launch rmcs-navigation'; sleep 2
   for p in nav2_lifecycle_manager nav2_bt_navigator nav2_planner \
            nav2_controller nav2_map_server foxglove_bridge static_transform_publisher; do
     pkill -f "$p"
   done
   sleep 2
   ps aux | grep -E 'nav2|ros2 launch|foxglove' | grep -v grep | wc -l   # → 0
   ```

2. **构建导航包及其依赖**
   ```bash
   bash -lc 'export PATH=/opt/cmake/bin:$PATH && cd /workspaces/RMCS/rmcs_ws \
     && source /opt/ros/jazzy/setup.bash && source install/setup.bash \
     && colcon build --packages-up-to rmcs-navigation --symlink-install'
   ```

3. **启动静态导航栈**（无需 C 板 / 雷达 / 定位）
   ```bash
   bash -lc 'cd /workspaces/RMCS/rmcs_ws && source install/setup.bash \
     && ros2 launch rmcs-navigation static.launch.yaml'
   ```

4. **验证节点与状态**
   ```bash
   ros2 node list
   for n in planner_server controller_server bt_navigator global_map local_map; do
     printf "%-20s %s\n" "$n" "$(ros2 lifecycle get /$n | head -1)"; done
   ros2 param get /planner_server GridBased.plugin
   ```

5. **测单次规划**（起点 = 机器人位置，终点取场地右侧需绕中央障碍）
   ```bash
   ros2 action send_goal /compute_path_to_pose nav2_msgs/action/ComputePathToPose \
     "{goal: {header: {frame_id: world}, pose: {position: {x: 20.0, y: 0.0, z: 0.0}}}, use_start: false}"
   ```

6. **测完整导航链路 + 频率**
   ```bash
   ros2 action send_goal /navigate_to_pose ... &      # 后台跑
   ros2 topic hz /plan
   ros2 topic hz /cmd_vel
   ```

7. **查代价地图**（订阅 `/global_costmap/costmap`，精确读特定坐标的代价值）

### 结果

**节点与状态**

| 检查项 | 结果 |
|---|---|
| 节点 | 15 个，无重复 |
| 生命周期 | `bt_navigator` / `planner_server` / `controller_server` / `global_map` / `local_map` **全部 `active [3]`** |
| 规划器 | `nav2_navfn_planner::NavfnPlanner` ✅ |
| 全局地图 | `Read map rmuc-v2.png: 292 X 161 map @ 0.1 m/cell` |
| 全局代价地图 | 292 × 161 @ 0.100，frame `world` |
| 局部代价地图 | 400 × 400 @ 0.040，frame `base_link`，**全 0（mock 空图）** |

**单次规划**（`ComputePathToPose`，起点 `world(0,0)` → 终点 `world(20,0)`）

| 指标 | 实测值 |
|---|---|
| `planning_time` | **1.03 ms** |
| 路径点数 | 823 |
| 路径长度 | **41.53 m** |
| 转折点数（相邻段夹角 > 5°） | **368** |
| `error_code` | 0（成功） |

> 直线距离仅 20 m，路径 41.53 m 是绕中央环形障碍所致。

**链路频率**

| 话题 | 实测 | 配置值 |
|---|---|---|
| `/plan` | **0.99 Hz** | 行为树 `RateController hz=1.0` |
| `/cmd_vel`（平均） | **19.9 Hz** | `controller_frequency=20.0` |
| `/cmd_vel`（峰值段） | ⚠️ **15.5 ~ 15.9 Hz** | 同上 |

**代价地图分布**（全局，已用修正版工具复测）

> 复测原因：`OccupancyGrid.data` 是 **`int8[]`**（有符号），而 nav2 代价是 0~254
> （无符号语义），所以 `254→-2`、`253→-3`、`255→-1`。首次读取时脚本没处理这点，
> 把 `100` 误标成"致命障碍"（真致命应是 `-2`）。现已用 [tools/costmap_probe.py](tools/costmap_probe.py) 复测。

| 类别 | 格数 | 占比 |
|---|---|---|
| 致命（254） | 0 | 0% |
| 内切（253） | 0 | 0% |
| 膨胀区（1~252） | 32305 | **68.7%** |
| **完全自由（0）** | 14707 | **31.3%** |
| 未知（255） | 0 | 0% |

**关键读数**：

| 世界坐标 | 代价 | 判定 |
|---|---|---|
| 机器人起点 `(0,0)` | 89 | 膨胀区 |
| 终点 `(20,0)` | 0 | 完全自由 |
| 场地中央 `(10,0)` | 100 | 膨胀区（首次误判为致命） |
| 左侧 `(-3,0)` | 88 | 膨胀区 |

⭐ **可用结论**：**只有 31.3% 的地图是"完全自由"**，其余 68.7% 都被膨胀覆盖。
这说明代价地图**相当保守**，规划器能用的"廉价空间"很窄
—— 直接解释了为什么路径容易贴在障碍边缘、给不出从容的走法。

**⚠️ 尚未解释的现象**（留待后续）：

1. 沿 x 轴 `y=0` 从 -4 到 +4 采样，代价几乎**恒定在 84~100**，没有梯度
   —— 而膨胀的代价必然随离障碍距离变化，均匀值不像膨胀产生的。
2. 按公式 $cost = 252 \cdot e^{-k(d - r_{in})}$ 反推：
   $k=0.5$、$r_{in}=0.1$ 时，代价 89 对应"距障碍 2.18 m"，
   但这**超过了 `inflation_radius`（0.5 m）**，逻辑上不应有代价。
3. 已用 `ros2 param get` 回读确认运行参数**确实是** `inflation_radius=0.5`、
   `cost_scaling_factor=0.5`、`resolution=0.1`、`footprint` 0.2 m 见方
   —— **不是"参数假生效"**。

> 这三条说明我对该层行为的理解还有缺口（可能涉及层内单位换算、与 static layer
> 数值的叠加方式等）。**不影响调参方向**（方向由源码公式确定，见
> [导航提交作业.md](导航提交作业.md) §2.3），但**具体数值不能凭公式硬算**，
> 要以实测为准。

**产物**

| 文件 | 内容 |
|---|---|
| `assets/navfn_baseline_path.png` | 基线路径可视化（可见锯齿） |
| `assets/navfn_baseline_raw.txt` | 原始输出，含 823 个路径点 |

### 问题

**① 节点重复实例 + 端口占用（已解决）**

首次启动 `ros2 node list` 里**每个节点出现两次**，且 `foxglove_bridge` 报：

```
Couldn't initialize websocket server: Bind Error
```

原因：上一次验证留下的孤儿进程（PID 54197 / 54203 / 54204）还在跑，
新实例又起了一遍；8765 端口被旧进程占用。

解决：按上面第 1 步清理后重启 → 节点无重复，`foxglove_bridge` 打印
`Server listening on port 8765`。

**② 控制环丢帧（待排查）**

持续导航时 `controller_server` 反复报：

```
Control loop missed its desired rate of 20.0000 Hz. Current loop rate is 15.5253 Hz.
Control loop missed its desired rate of 20.0000 Hz. Current loop rate is 15.8733 Hz.
Control loop missed its desired rate of 20.0000 Hz. Current loop rate is 15.7266 Hz.
Control loop missed its desired rate of 20.0000 Hz. Current loop rate is 15.8165 Hz.
Control loop missed its desired rate of 20.0000 Hz. Current loop rate is 15.8606 Hz.
```

配置 20 Hz，实测只有 **15.5 ~ 15.9 Hz**（缺约 21%）。

可能原因（未验证）：
1. MPPI 计算量大：`batch_size: 1400`、`time_steps: 50`、5 个 critic；
2. 容器 CPU 资源有限，无 GPU 加速；
3. `foxglove_bridge` 同时发布 49 个 channel。

> 注意：这是**局部控制器**丢帧，不是全局规划器的问题，但与"无明显卡顿"的验收直接相关。

**③ 静态环境下 `navigate_to_pose` 必然失败（预期行为，非故障）**

```
[bt_navigator]      Begin navigating from current location (0.00, 0.00) to (20.00, 0.00)
[controller_server] Received a goal, begin computing control effort.
[controller_server] Passing new path to controller.     ← 持续约 1 Hz
[controller_server] Failed to make progress            ← 4 秒后
[controller_server] [follow_path] [ActionServer] Aborting handle.
                      → 清局部 + 全局代价地图 → 重试（重复 8 次）
[bt_navigator]      Goal failed
```

原因：静态 TF 使机器人永不移动 → `SimpleProgressChecker`（`movement_time_allowance: 4.0`）
检测不到位移 → 判"无进展" → 恢复重试 → `RecoveryNode` 重试耗尽 → 失败。

解决方向：用 `tools/virtual_chassis.py` 让 `base_link` 动起来（见 [任务规划.md](任务规划.md)）。

### 结论

| 项 | 结论 |
|---|---|
| 静态栈可用性 | ✅ 无需机器人即可跑通完整 Nav2 链路 |
| 基线确认 | ✅ 现用 `NavfnPlanner`，与细则表述一致 |
| 规划质量 | ⚠️ 41.53 m / **368 转折** —— 锯齿折线实测确认 |
| 规划耗时 | 1.03 ms |
| 链路频率 | `/plan` 0.99 Hz ✅；`/cmd_vel` 峰值段丢帧至 15.8 Hz ⚠️ |
| 环境坑 | 残留进程 → 节点重复 + 端口占用；**每次实验前必须清理** |

**下一步**
1. 换 `SmacPlanner2D`，用同一组起终点复测对比转折点数
2. 用 `tools/virtual_chassis.py` 让画面动起来，在 Foxglove 里观察动态过程
3. 排查控制环丢帧：降 `batch_size`（1400 → 800）看频率是否恢复

---

## 模板（后续实验照此填写）

```
## YYYY-MM-DD HH:MM – HH:MM · 一句话主题

### 执行
（命令原文）

### 结果
（数据表）

### 问题
（现象 + 原因 + 解决/待办）

### 结论
（结论 + 下一步）
```
